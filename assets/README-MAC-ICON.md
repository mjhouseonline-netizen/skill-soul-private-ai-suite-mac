# Mac Icon

If you want a polished Mac app icon, place:

- `app.icns`

in this `assets/` folder.

`build-mac.sh` will use it automatically if present.

If it is missing, the build still succeeds and falls back to the default icon.
